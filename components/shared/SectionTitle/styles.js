import styled from "styled-components";

const SectionTitle = styled.div`
  position: absolute;

  top: 24px;
  padding-left: 20px;

  & > :first-child {
    margin-bottom: 4px;
  }
`;

export { SectionTitle };
